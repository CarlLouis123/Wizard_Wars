# PokeLike - Template Driven (Gemini-ready)

Everything is drawn from templates (JSON/YAML). Optional Gemini dialogue.

## Setup
```
pip install -r requirements.txt
python game/main.py
```
Toggle Gemini with **G** in-game. Put API key in `config/api_key.txt` or set `GOOGLE_API_KEY`.
